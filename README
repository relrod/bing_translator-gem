To use this rubygem:
    $ sudo gem install bing_translator

    require 'rubygems'
    require 'bing_translator'
    translator = BingTranslator.new :api_key => 'Your_API_Key'
    spanish = translator.translate 'Hello. This will be translated!', :from => 'en', :to => 'es'
